spring:
  application:
    name: service-items

server:
  port: 8002

eureka:
  client:
    service-url:
      #      Always enter the path where the eureka server is located.
      defaultZone: http://localhost:8761/eureka